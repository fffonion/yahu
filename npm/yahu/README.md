# @fffonion/yahu

Install the Yahu desktop application globally:

```bash
npm install -g @fffonion/yahu
```

Then start it with:

```bash
yahu --help
```

The package includes the release binaries for Linux x64/arm64, macOS arm64, and Windows x64. The global `yahu` command selects the matching binary for the current platform.
